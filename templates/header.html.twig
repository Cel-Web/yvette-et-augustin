<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Y & A</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarColor03">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link"href="{{ path('home') }}">Accueil</a>
      </li>
      <li class="nav-item">
        <a class="nav-link"href="{{ path('concept') }}">Le concept</a>
      </li>
      <li class="nav-item">
        <a class="nav-link"href="{{ path('presentation') }}">Qui sommes nous ?</a>
      </li>
    </ul>
    <ul class="navbar-nav my-2 my-lg-0">
      {% if app.user %}
      <li class="nav-item dropdown">
        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
          Recettes
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
          <a href="{{ path('recette_all') }}" class="dropdown-item">Les recettes</a>
          <a href="{{ path('recette_add') }}" class="dropdown-item">Ajouter une recette</a>
          <a href="{{ path('recette_user') }}" class="dropdown-item">Mes recettes proposées</a>
          <a href="{{ path('favoris') }}" class="dropdown-item">Mes recettes préférées <i class="far fa-heart"></i></a>
        </div>
      </li>
      <li class="nav-item dropdown">
        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
          aromatheque
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
        <a href="{{ path('aromatheque') }}" class="dropdown-item">Mon aromatheque</a>
          <a href="{{ path('aromatheque_add') }}" class="dropdown-item">Ajouter un produit à mon aromatheque</a>
        </div>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('produit_new') }}">Produit</a>
      </li>
      <li class="nav-item dropdown">
        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
          Mon compte
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
          <a href="{{ path('account_edit') }}" class="dropdown-item">Modifier mon compte</a>
          <a href="{{ path('account_password') }}" class="dropdown-item">Modifier mon mot de passe</a>
        </div>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('account_logout') }}">Se déconnecter</a>
      </li>
      {% else %}
      <li class="nav-item">
        <a class="nav-link" href="{{ path('account_login') }}">Se connecter</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('account_register') }}">S'inscrire</a>
      </li>
      {% endif %}
    </ul>
  </div>
</nav>
{% for label, messages in app.flashes %}
<div class="container pt-5">
  <div class="alert alert-dismissible alert-{{ label }}">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
      {% for message in messages %}
        <p>{{ message | raw }}</p>
      {% endfor %}
  </div>
</div>
{% endfor %}