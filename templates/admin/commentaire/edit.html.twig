{% extends 'admin/base.html.twig' %}

{% block title %}Admin - voir un commentaire{% endblock %}

{% form_theme form _self %}

{% block body %}
    <!-- Page Heading -->
    <div class="row">
        <div class="col-md-8">
            <h1 class="h3 mb-0 text-gray-800">Modifier le commentaire de <b>{{commentaire.user.prenom}}</b></h1>
        </div>
        <div class="col-md-4 text-right">
            <a href="{{ path('admin_commentaire_index') }}" class="btn btn-primary">Retour à la liste des commentaire</a>
        </div>
    </div>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Modifier ce commentaire</button>
    {{ form_end(form) }}
{% endblock %}

